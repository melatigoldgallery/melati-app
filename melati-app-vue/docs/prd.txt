07-PRD-MULTI-FLOOR-SINGLE-APP.md
 perbaiki prd agar L1 dan L2 memiliki fitur yang sama namun scope data akses yang terpisah sesuai lantai. saat ini di Lt 1 terdapat role supervisor, staff, admin, dan hrd di lantai 2 cukup role supervisor dan admin saja. untuk supervisor samakan username dan password yang sudah ada untuk role admin Lt buat menjadi admin dan password adminyoung. pastika supervisor Lt 2 memiliki full akses seperti di LT 1 agar bisa mengatu hak akses terhadap menu. saya ingin saat pertama muncul halaman menampilkan section untuk memilih Lt 1 atau Lt 2 ketika sudah memilih salah satu langsung muncul form login dan diarahkan menampilkan data sesuai floor. analisis dan perbaiki kode yang diperlukan, agar lebih ringkas efektif dan efisien.
 Analisis selesai. Saya belum mengubah kode dulu.

Gap utama yang ditemukan

PRD masih kontradiktif dengan requirement baru:

“fitur berbeda per lantai” di 07-PRD-MULTI-FLOOR-SINGLE-APP.md (line 5)

L2 tidak bisa akses servis/absensi di 07-PRD-MULTI-FLOOR-SINGLE-APP.md (line 159)

L2 dibatasi menu tertentu di 07-PRD-MULTI-FLOOR-SINGLE-APP.md (line 525)



Login belum punya step pilih lantai:

Form langsung username/password di LoginView.vue (line 16)

auth.login() tidak menerima floorId di LoginView.vue (line 89)



Auth belum punya konteks floor:

State hanya userRole dan accessPages di auth.js (line 67)

supervisor selalu full akses global (belum floor-aware) di auth.js (line 79)



Cloud Function login username belum floor-aware:

Query langsung users/{username} di functions/index.js (line 425)

Custom token claim belum membawa floor context di functions/index.js (line 452)



Data operasional belum di-scope per lantai:

Banyak service masih query koleksi global (servis, order_online, dll), contoh:

servis-service.js (line 106),

order-online-service.js (line 172)



Rules belum enforce isolasi lantai:

Firestore default allow read, write: if request.auth != null; di firestore.rules (line 63)

Storage rules juga belum floor-scoped di storage.rules (line 90)



Kelola user masih global (belum per lantai):

Role form masih umum semua lantai di KelolUserView.vue (line 130)

Sinkronisasi userRoles/{email} belum floor-aware di KelolUserView.vue (line 560)




Rencana implementasi (ringkas + efisien)

Revisi PRD 07 agar L1 dan L2 fitur sama, beda hanya scope data + matriks role per lantai.
Tambah floor context terpusat (activeFloor) dan persist di session.
Ubah Login jadi 2 tahap: pilih Lt 1 / Lt 2 lalu tampil form login.
Ubah loginWithUsername Cloud Function menerima floorId.
Ubah model akun jadi floor-aware agar username sama bisa dipakai lintas lantai dengan konteks floor.
Batasi role berdasarkan floor:

L1: supervisor, admin, staff, hrd

L2: supervisor, admin



Seed/update akun:

Supervisor L2 pakai username/password supervisor existing.

Admin L2: admin / adminyoung.



Floor-scope query/write di service (pakai helper supaya refactor ringkas, tidak duplikasi).
Update Firestore/RTDB/Storage rules agar enforce floorId.

Konfirmasi dan saya lanjut eksekusi penuh sesuai rencana di atas.

Implementasi sudah saya lanjutkan dan sudah saya harden untuk kasus multi-floor.

Perubahan utama:


Login sekarang wajib dalam konteks lantai terpilih (Lt 1 / Lt 2), lalu baru form login.

LoginView.vue (line 16)



Auth store sekarang memvalidasi user profile harus cocok dengan lantai yang dipilih, termasuk login email (bukan hanya username).

Jika tidak cocok, login ditolak dengan error auth/floor-user-mismatch.

auth.js (line 226)

auth.js (line 425)



Cloud Function loginWithUsername sekarang:


wajib menerima floorId,

hanya mencari user sesuai floor,

menolak mismatch floor,

menolak role yang tidak valid untuk floor (L2 hanya supervisor/admin).

index.js (line 437)

index.js (line 491)




PRD sudah dirapikan agar konsisten dengan requirement terbaru (L1/L2 fitur sama, tanpa aturan lama “L2 no-storage”).

07-PRD-MULTI-FLOOR-SINGLE-APP.md (line 43)

07-PRD-MULTI-FLOOR-SINGLE-APP.md (line 322)



